import type { IconType } from "react-icons";

export type priceItemsProps = {
    title: string;
    price: string;
    description: string
}

export type checkListProps ={
    icon: IconType;
    definition: string
}